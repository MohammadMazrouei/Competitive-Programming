#include <bits/stdc++.h>
using namespace std;

void solve() {
    stack<int> st;    

    // push, emplace, pop, top
    // size, empty
}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    //cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}
