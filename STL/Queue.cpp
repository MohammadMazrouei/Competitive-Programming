#include <bits/stdc++.h>
using namespace std;

void solve() {
    // --- Initialization ---
    queue<int> q;    
    queue<int, deque<int>> q1;
    queue<int, list<int>> q2;

    // --- Capacity ---
    // size(), empty()

    // --- Element access ---
    // front(), back()

    // --- Modifiers ---
    // push(value), emplace(args...)
    // pop()
}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    //cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}
