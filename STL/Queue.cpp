#include <bits/stdc++.h>
using namespace std;

void solve() {
    // queue<int, deque<int>> q1;
    // queue<int, list<int>> q2;
    queue<int> q;    

    // --- Capacity ---
    // size(), empty()

    // --- Element accessa ---
    // front(), back()

    // --- Modifiers ---
    // push(value), emplace(args...)
    // pop()
}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    //cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}
